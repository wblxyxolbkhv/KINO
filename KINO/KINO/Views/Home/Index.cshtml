@{
    ViewBag.Title = "Главная страница";
    IEnumerable<KINO.Models.Film> films = ViewBag.Films;
    IEnumerable<KINO.Models.Hall> halls = ViewBag.Halls;

    <h1>Популярные фильмы</h1>
    Html.RenderPartial("FeaturedFilms", films);

    <h1>Наши залы</h1>    
    Html.RenderPartial("HallsSlideshow", halls);
}
